/*
 * Лабораторна робота №1
 * Дисципліна: Алгоритмізація та програмування
 * Виконала: студентка групи 318, Сергійчук Ольга
 * Завдання: Begin1 (Обчислення периметра квадрата)
 */

#include <iostream>

using namespace std;

int main() {
    // Встановлюємо українську локаль для коректного виведення текстів у консолі
    setlocale(LC_ALL, "Ukrainian");
    
    cout << "--- ЗАДАЧА Begin1: Периметр квадрата ---" << endl;
    
    double a_sq, perimeter;

    // Введення довжини сторони квадрата з клавіатури
    cout << "Введіть сторону квадрата a (> 0): ";
    cin >> a_sq;

    // Розрахунок периметра за формулою: P = 4 * a
    perimeter = 4.0 * a_sq;

    // Виведення отриманого результату
    cout << "Результат: Периметр квадрата дорівнює " << perimeter << endl;

    return 0;
}
